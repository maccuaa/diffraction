{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>maccuaa/renovate-config"],
  "packageRules": [
    {
      "matchDepNames": ["diff", "jsdiff"],
      "groupName": "diff",
      "description": "Keeps the npm 'diff' devDependency (used by tests) and the cdnjs 'jsdiff' <script> tag (used by the browser, see index.html) on the same version, in one PR."
    }
  ]
}
